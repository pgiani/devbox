// Render list of bought pizzas as horizontal tiles (PizzaList component)
// create Pizza component where:
//         - Size is a header
//         - Toppings count is a green number
//         - Checkbox that is checked if there is any sauce
import React from "react";
import foodList from "./foodList";

export default function PizzaList() {}
